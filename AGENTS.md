# 开发规范与注意事项 (AGENTS)

这份文档旨在为参与开发（包括 AI Agents 如 Codex、Copilot 等）提供明确的指导原则和注意事项，以确保代码的规范性和一致性。

## 1. 核心技术栈
- **构建工具**: Vite
- **包管理器**: npm
- **前端框架**: React
- **编程语言**: JavaScript / JSX
- **部署平台**: Cloudflare Pages

## 1.1 MD3 Web 组件实现依据
- 涉及 Material Design 3 Web 组件（`@material/web`）的实现、样式覆写、token 使用与交互行为判断时，默认以官方文档 **https://material-web.dev/** 为首要依据。
- 如与历史实现存在冲突，优先对照官方文档再决定是否兼容保留旧行为，并在代码注释或变更说明中写明取舍原因。

## 1.2 仓库与站点边界

- 本仓库只负责部署到 `https://www.vrclearn.com` 的 VRCLearn 主站。
- VRCLearn 与 ALCOMD 的文档及共享文档站属于 `VRCLearn-Docs` 仓库，并部署到 `https://docs.vrclearn.com`。
- 本仓库不得在构建阶段拉取、编译或内嵌文档内容；主站保留 `/docs` 文档入口页，只通过该页面中的普通链接跳转到文档子域。
- VRCLearn 主站名称、规范域名、仓库地址、文档入口和统计 ID 等稳定信息统一维护在根目录的 `site.config.json`，不得通过部署环境变量或业务代码重复配置。

## 2. 绝对禁止修改的文件/目录
- **`public/theme/`**: 该目录下的主题文件由 Material Theme Builder 导出，**必须完全按照导出时的原样使用，绝不允许进行任何手动修改**。业务/UI CSS 中的可见主题颜色必须使用这些主题文件提供的 CSS 变量，除非属于第 6 节明确允许的静态资源颜色、透明色或遮罩例外；不得在业务样式中重新定义主题色或任意硬编码可见 UI 颜色。
  - **重要引入规范**: 为遵循 Vite 构建管道的设计，`public/` 目录下的静态资源（包括主题 CSS）应仅通过 HTML `<link>` 标签在全局引入（如 `<link rel="stylesheet" href="/theme/light.css">`、`<link rel="stylesheet" href="/theme/dark.css">`）。**严禁在 JS 或 CSS 代码中通过 `import` 或 `@import` 引入 `public/` 下的资源**，以避免破坏打包流程。

## 3. 设计规范与 UI 布局
- **设计语言**: 遵循 Google Material Design 3 (MD3) 设计规范。
- **无边框美学**: UI 布局组件（如 header, nav, main container 等）**不要使用可见边框 (borders)**，以保持无边框的整体美感。
- **边框例外**: 用于滚动条裁切、焦点态、透明留白或浏览器兼容技巧的非视觉边框可以作为例外，但不得破坏整体无边框观感。

## 4. 本地化与目标环境
- **主要语言**: 简体中文 (zh-CN)。
- **目标浏览器**: 只需确保在 **Chrome** 浏览器下正常运行，无需针对其他浏览器进行特定优化。
- **No-JS 支持**: 应用程序不需要支持禁用 JavaScript 的用户，无需为 `<noscript>` 环境提供回退逻辑或设计考量。

## 5. 响应式策略
- **统一指标**: 通过统一外边距 (margins)、内边距 (padding) 和字体大小等指标，最大化桌面端和移动端的一致性。
- **最小化特定优化**: 仅在绝对必要（如为了基本可读性）的情况下，才使用针对移动端的特定 CSS 优化。

## 6. 资源与字体加载
- **本地托管**: 所有资源（包括字体，如 `@fontsource/roboto` 和 `material-symbols`）必须本地托管和配置。
- **禁止使用 CDN**: 避免依赖外部 CDN（如 Google Fonts），以确保在外部服务不可用的地区也能正常访问。
- **静态资源颜色例外**: 作为第 2 节主题色规则的明确例外，SVG 图标、favicon、品牌标识或第三方平台图标可以保留资源文件本身所需的固定颜色；如需新增或替换，必须保持资源来源与设计语义清晰。
- **遮罩与透明色例外**: 作为第 2 节主题色规则的明确例外，透明背景、滚动条透明留白、遮罩叠层等非可见主题色用途可使用 `transparent` 或带 alpha 的颜色值；凡是表达可见 UI 主题颜色的场景，仍必须使用 MD3 主题变量。

## 7. 代码格式化
- **缩进**: 在整个项目中严格统一使用 **4 个半角空格** 进行缩进。**例外情况包括：1. 该文件在技术上无法修改为 4 个空格缩进；2. 该文件属于绝对禁止修改的目录（如 `public/theme/` 中的文件）；3. 由包管理器自动生成或维护的 JSON 配置文件（如 `package.json`, `package-lock.json` 等），为遵循社区惯例和避免自动化工具冲突，允许使用 2 个空格缩进。**除上述例外，其他文件均必须遵循 4 个空格缩进规则。

## 8. 提交规范
- **无测试残留**: 确保不要将临时的实验性测试构件、无用的检查脚本或临时测试依赖包含在最终的提交中，保持代码库的整洁。**（注意：此规则仅针对临时性内容，正式的、用于长效维护的端到端测试或单元测试用例及框架是允许且鼓励保留的。）**
